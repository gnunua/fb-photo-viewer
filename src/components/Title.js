import React from "react";

const Title = ({title}) => {
    return (
        <div>
            <h3 className="text-primary">
                {title}
            </h3>
        </div>
    )
};

export default Title;
